repos:
  - repo: local
    hooks:
      - id: ruff
        name: ruff lint
        entry: uv run ruff check .
        language: system
        pass_filenames: false
      - id: pytest
        name: pytest + coverage gate
        entry: uv run pytest
        language: system
        pass_filenames: false
