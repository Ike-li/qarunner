# qarunner 需求文档

## 0. 文档控制

### 0.1 元数据

| 项 | 内容 |
|----|------|
| 产品 | qarunner |
| 文档版本 | 2.0 |
| 状态 | 现行能力基线（描述当前产品应持续满足的需求） |
| 所有者 | 仓库维护者（git: Ike-li）——需求的最终裁决与批准人 |
| 生效日期 | 2026-07-11 |
| 基线锚 | 以本文所在 git 提交为基线；变更经评审合入主分支即形成新基线 |
| 复核节奏 | 需求变更随改随审；每季度整体复核一次「本文 ↔ 实现」一致性 |

**本文职责**：说清楚要解决什么问题、给谁用、必须做到什么、可选做什么、明确不做什么、怎样算合格。
**不写**：实现方案、类名、接口字段表、人物故事。

### 0.2 关联文档与职责分工

| 文档 | 内容 | 冲突时 |
|------|------|--------|
| [`DIRECTION.md`](DIRECTION.md) | 产品是什么、四支柱、边界（**唯一方向源**） | 方向以它为准 |
| **本文** | 需求：目标、对象、功能、规则、验收、追溯 | 行为以可运行系统 + 本文验收为准 |
| [`FEATURES.md`](FEATURES.md) | 实现侧能力对照 | 与本文冲突按 §0.4 处理 |
| [`API_REFERENCE.md`](API_REFERENCE.md)、运行时 `/openapi.json` | 接口字段、状态码 | 字段以 OpenAPI 为准，并回写 API 文档 |
| [`deployment.md`](deployment.md) | 怎么装、怎么配 | — |
| [`METRICS.md`](METRICS.md) | 质量度量的公式、目标与触发动作 | 判定口径以本文 §3.3 + 源码为准 |

### 0.3 需求编号规范

- **格式**：`前缀-序号`（如 `RN-3`）。ID 全局唯一，**一经基线化即冻结**：不复用、不重排；需求废弃时保留条目并标注「已废弃 (版本)」。新增需求取该前缀下一个未用序号。
- **前缀表**：GO 产品目标 · SU 套件接入 · CR 凭证 · PF 执行配置 · RN 运行 · RP 报告/日志/产物 · DF 跨次对比 · SC 调度 · NT 通知 · AI 失败诊断 · UI 界面与 API · BR 业务规则 · NF 非功能 · AS 假设依赖 · RK 风险 · OP 开放项 · V 验收。
- **优先级**：**P0** 主路径/安全底线，缺了不能当可用产品 · **P1** 完整内部工具体验，应具备 · **P2** 增强，可关可降级。
- **实现状态约定**：本文全部需求默认为**已实现并受仓库自动化回归保护**；例外（部分实现、待校准）必须显式标注并登记到 §10 开放项。
- **验证方式约定**：合格标准默认由自动化测试验证（仓库既有门禁）；只能人工验证的条目须在合格标准中注明「人工评审」或「演示」。**新增需求必须同时给出可执行的验证方式，否则不得合入。**

> 说明：v1.x 曾用单字母编号（S1/P1/R1…），其中 Profile 需求 `P1–P3` 与优先级 `P0–P2` 撞名。v2.0 借文档尚未被外部引用的唯一窗口一次性重编为双字母前缀，此后 ID 冻结。旧→新映射见 §13 修订记录。

### 0.4 变更管理

| 变更级别 | 例子 | 流程 |
|----------|------|------|
| **方向级** | 改一句话定位、四支柱、§9 不做清单 | 先改 [`DIRECTION.md`](DIRECTION.md) 及其派生清单，再回改本文；缺一不可 |
| **需求级** | 新增/修改/废弃某条需求、改优先级或合格标准 | 改本文对应条目 + §12 追溯矩阵 + §13 修订记录，经所有者评审合入（合入即批准），文档版本号 +0.1 |
| **实现级** | 重构、换库、调实现参数（不触碰本文任何合格标准） | 不动本文；若实现与本文冲突，以「先修文档或先修实现」的显式决定收敛，禁止长期带病并存 |

---

## 1. 背景与目标

### 1.1 要解决的问题

团队有 pytest / Playwright 回归套件，但常见情况是：

1. 跑测入口散（本机、零散脚本、聊天群贴日志），结果难复用。
2. 只能看「这一次过没过」，难判断相对上次是**新挂、修好、还是老 flaky**。
3. 需要定时回归，又不想上完整 CI/QA 平台。
4. 测试代码和依赖在宿主上直跑，风险和误操作成本高。

### 1.2 产品目标

| ID | 目标 | 合格判据 | 度量锚 |
|----|------|----------|--------|
| GO-1 | 套件接得进、跑得动 | 新套件从接入到首个终态 Run 全程在平台内完成，无需改平台代码（V1） | [`METRICS.md`](METRICS.md) #4 时长、#5 跑量 |
| GO-2 | 配置可复用、可定时 | 同一 Profile 可反复一键触发、可被调度引用，cron 到点自动出 Run（V1） | METRICS #5 |
| GO-3 | 结果可解释 | 任一失败 Run 不出平台即可定位到用例级失败与日志/报告证据（V1） | — |
| GO-4 | 跨次可对比 | 同范围两次 Run 能区分新增失败 / 已修复 / 持续失败，可疑用例可查历史（V2） | METRICS #1 通过率、#2 flaky |
| GO-5 | 默认更安全 | 默认容器执行；任何越权读写被拒（V3、V4） | — |
| GO-6 | 部署够简单 | 按 [`deployment.md`](deployment.md) 标准路径完成单节点部署且健康检查通过（V6）；可选能力全关不影响主路径（V5） | — |

### 1.3 一句话（引用方向，不扩写）

自托管、单节点的测试执行 + 调度 + 回归对比服务。完整表述见 [`DIRECTION.md`](DIRECTION.md)。

### 1.4 范围

| 做 | 不做 |
|----|------|
| 接入套件、执行、调度、报告与日志、跨次对比 | CI 流水线、用例库/缺陷管理 |
| 账号与对象权限、Web 控制台 + API | 多租户 SaaS、组织/项目空间 |
| 可选：飞书通知、AI 失败诊断 | AI 自动改代码、多活水平扩展（默认形态） |

---

## 2. 使用对象与权限

### 2.1 谁用

| 角色 | 典型工作 | 系统权限 |
|------|----------|----------|
| 测试/开发（user） | 接入套件、配 Profile、触发/定时跑、看结果与对比 | 只管理自己的资源 |
| 管理员（admin） | 开账号、清磁盘、看全量数据、排障 | 全局能力 + 用户管理 |
| 运维 | 部署、备份、配环境变量 | 部署面；应用内通常用 admin |
| 脚本/集成 | Token 调 API | 与对应用户权限相同 |

### 2.2 权限总规则

1. **所有者范围**：user 只能读写自己名下对象——Suite、Credential、Profile、Run、Schedule，及其派生数据（diff 基线、趋势、用例历史）。
2. **列表**：user 只看到自己的，不报错。
3. **单条越权**：他人资源不能读改；「没权限」与「不存在」可区分（码表见 API 文档）。
4. **admin**：可跨用户查看与管理（用户管理、清理等以功能表为准）。
5. **登录**：用户名 + 密码；之后用 Bearer Token 或同源 Cookie。
6. **禁止**：URL 查询参数带 Token 当认证。
7. **生产**：UI 与 API **同源**；Cookie 为 HttpOnly + SameSite=Strict，并开启 Secure（HTTPS）。

### 2.3 账号管理

| 能力 | 要求 |
|------|------|
| 登录 / 登出 / 当前用户 | 必须有；Token 有效期可配（默认 24 小时） |
| 用户 CRUD（角色、密码） | 仅 admin |
| 删自己 | 禁止 |
| 去掉最后一个 admin（降级或删除） | 禁止 |
| 登录防爆破 | 同「用户名 + IP」连续失败达阈值（默认 5 次，可配，0=关闭）后指数退避锁定（60 秒起，上限 900 秒） |

---

## 3. 术语与核心对象（读需求前先对齐词）

### 3.1 核心对象

| 对象 | 含义 | 关键关系 |
|------|------|----------|
| 套件 Suite | 接进来的测试代码（Git 或本地） | 被 Profile / Run 引用 |
| 凭证 Credential | 私有仓 Token | 仅元数据可见，secret 不回显 |
| 配置档 Profile | 「怎么跑」的配方 | 可一键触发；可被调度引用；可挂通知地址 |
| 运行 Run | 一次异步执行及结果 | 来自即席参数 / Profile / 调度 / 重跑 |
| 调度 Schedule | cron + 时区 + 绑定 Profile | 到点或手动立即触发产生 Run |
| 用例结果 | 单条用例在某次 Run 的结果 | 支撑 diff / 历史 / flaky |

### 3.2 Run 状态机

排队中（queued）→ 执行中（running）→ **终态**：完成（completed）| 失败（failed）| 超时（timeout）| 已取消（cancelled）。
终态不可再取消、不可再迁移。

### 3.3 关键术语（判定口径，与源码一致）

| 术语 | 精确定义 |
|------|----------|
| **同范围** | 两次 Run 的 `profile_id`、套件路径、runner、编译后参数（marker / 选中文件 / 附加参数在创建时折叠进参数串）**四者完全一致**。同一套件同参数下，走 Profile 与即席运行互不同范围 |
| **基线** | 与本次 Run 同范围、状态为「完成」、创建时间早于本次的**最近一次** Run；不存在则「无基线」 |
| **失败** | 用例状态为 failed 或 error；skipped 与 passed 不算失败（diff 与 flaky 同口径） |
| **flaky（不稳定）** | 同一用例（套件+名称）按时间序把状态二值化为失败/非失败后：观测次数 ≥ 4 **且**相邻翻转次数 ≥ 3（两阈值均可配）。单调劣化或单次「挂→修」不算 |
| **即席运行** | 不经 Profile、直接以参数创建的 Run |
| **在途** | 状态为排队中或执行中的 Run |

---

## 4. 主路径（必须跑通）

以下三条是验收底线。细节表见第 5 节。

### 4.1 首次跑通

接入套件（Git 或本地）→ 必要时准备依赖 → 建 Profile → 触发 → 进入终态 → 能看汇总与日志/报告。

### 4.2 日常回归

定时或手动触发同一 Profile → 与上次**同范围**结果对比 → 能分清新增失败 / 已修复 / 持续失败 → 可疑用例能看历史是否反复跳。

### 4.3 权限不串

用户 A 的 Run、配置、凭证、对比基线数据，用户 B 不可见、不可用。

---

## 5. 功能需求

「合格标准」是测试与评审依据；默认由自动化测试验证（§0.3 约定）。

### 5.1 套件接入（SU）— P0/P1

| ID | 需求 | 优先级 | 说明 | 合格标准 |
|----|------|--------|------|----------|
| SU-1 | 列出套件 | P0 | 看到可跑集合 | 登录后可列出 |
| SU-2 | Git 接入 | P0 | 浅克隆指定 ref；可绑凭证 | 合法仓成功；非 `https://` / `git@` 形态一律拒绝 |
| SU-3 | 本地接入 | P1 | 受信环境链本地目录 | 合法路径成功 |
| SU-4 | 更新 | P1 | Git pull（复用绑定凭证） | 仅所有者或 admin |
| SU-5 | 准备依赖 | P1 | 如 npm ci | 失败必须明确失败 |
| SU-6 | 文件树与标记 | P1 | 选文件、pytest marker / Playwright tag | 能用于选择性执行 |
| SU-7 | 删除 | P1 | 删套件 | 仅所有者或 admin；删后不可见 |
| SU-8 | 路径安全 | P0 | 防穿越 | 套件名含 `/`、`\` 或前导 `.` 被拒；危险路径不能执行到未授权内容 |

### 5.2 凭证（CR）— P0/P1

| ID | 需求 | 优先级 | 说明 | 合格标准 |
|----|------|--------|------|----------|
| CR-1 | 保存 HTTPS Token | P1 | 私有仓用 | 加密存储 |
| CR-2 | secret 不回显 | P0 | 查询永不返回明文 | 任何响应不含密钥 |
| CR-3 | 安全注入 | P0 | 用于 clone/pull | 不进 argv、URL、普通日志 |
| CR-4 | 所有者范围 | P0 | 各用各的 | 不能用别人凭证 |

### 5.3 执行配置 Profile（PF）— P0/P2

| ID | 需求 | 优先级 | 说明 | 合格标准 |
|----|------|--------|------|----------|
| PF-1 | 增删改查 | P0 | 保存跑法 | 所有者可管；他人不能读改 |
| PF-2 | 一键触发 | P0 | 按配方建 Run | 新 Run 关联该 Profile |
| PF-3 | 通知地址 | P2 | 飞书 Webhook | 仅 `https://`；主机名解析为环回/私网地址的一律拒存 |

Profile 至少能表达：套件、runner、选择范围（文件/标记）、附加参数、环境变量、执行器、超时。

### 5.4 执行 Run（RN）— P0/P1

| ID | 需求 | 优先级 | 说明 | 合格标准 |
|----|------|--------|------|----------|
| RN-1 | 异步执行 | P0 | 提交即受理 | 有排队/执行中，最终唯一终态 |
| RN-2 | 即席参数 | P0 | 不建 Profile 也能跑 | 危险参数/路径拒绝 |
| RN-3 | 默认 Docker | P0 | 默认容器跑 | 默认执行器为 docker；隔离底线见 NF-3 |
| RN-4 | subprocess 受控 | P0 | 宿主进程内执行的退路 | 默认仅 admin；非 admin 需显式全局开关 |
| RN-5 | pytest + Playwright | P0 | 两套 runner | 均有用例级汇总；Playwright 产物平台内可取 |
| RN-6 | 取消 | P0 | 停掉在途任务 | 终态=已取消；进程/容器终止、并发槽释放 |
| RN-7 | 重跑 | P1 | 按原参数新建 | 新 ID；原记录不变 |
| RN-8 | 全局并发受限 | P0 | 防止打满宿主 | 同时执行数不超上限（默认 4，可配）；到限的新 Run 排队而非丢弃 |
| RN-9 | 单用户在途上限 | P0 | 防单人刷爆 | 在途数超上限（默认 20，可配，0=不限）即拒绝并返回限额错误；admin 豁免 |
| RN-10 | 超时 | P0 | 防挂死 | 达到超时（默认 30 分钟，上限 24 小时，可配）进入超时终态 |
| RN-11 | 查询列表/详情 | P0 | 含汇总、时间、日志尾等 | 按所有者规则 |
| RN-12 | 锁定 | P1 | 保护关键证据 | 清理跳过锁定 |
| RN-13 | 删除 Run | P1 | 删记录与产物 | 在途或锁定不可删（冲突错误）；删除彻底 |
| RN-14 | 清理产物 | P1 | admin 按保留天数清 | 非 admin 不能清；只清未锁定过期项的物理产物，元数据保留 |
| RN-15 | 崩溃恢复 | P0 | 单实例重启 | 重启后无永久排队/执行中（遗留标为失败） |
| RN-16 | 优雅停机 | P1 | 尽量落盘 | 停机有缓冲期（默认 30 秒，可配）；缓冲内完成的 Run 数据一致 |

**Playwright 产品约定**：结果与产物目录由平台接管，用户参数不得把产物写到平台管不到的地方。

### 5.5 报告、日志、产物（RP）— P0/P1

| ID | 需求 | 优先级 | 说明 | 合格标准 |
|----|------|--------|------|----------|
| RP-1 | 汇总 | P0 | 通过/失败/跳过/错误/耗时/通过率 | 跑完可查看 |
| RP-2 | 用例级落库 | P0 | 为跨次对比供数 | 每条用例的套件/名称/状态/耗时/信息逐条持久化 |
| RP-3 | Allure | P0 | HTML 报告（环境具备时） | 有则能开；无则状态明确 |
| RP-4 | 实时日志 | P0 | 执行中可看 | 流式持续输出；每流仅留存末尾 256KB 防撑爆 |
| RP-5 | 产物 | P1 | 列表/下载/打包 | 不可读出该 Run 目录之外的内容 |

### 5.6 跨次对比（DF，核心差异）— P0/P1

| ID | 需求 | 优先级 | 说明 | 合格标准 |
|----|------|--------|------|----------|
| DF-1 | 基线 diff | P1 | 本次 vs 基线（§3.3） | 五桶齐备：新增失败、已修复、持续失败、新增用例、消失用例 |
| DF-2 | 同范围才比 | P0 | 范围定义见 §3.3 | 范围不一致 → 明示「无基线」，禁止乱分桶（BR-9） |
| DF-3 | 基线也讲权限 | P0 | 只用自己有权的历史 | 非 admin 的基线候选不含他人 Run |
| DF-4 | 通过率趋势 | P1 | 套件（可限 Profile）历史曲线 | 仅统计「完成」Run；无数据为空列表 |
| DF-5 | 用例历史 | P1 | 单用例多次状态 | 可见按时间序列 |
| DF-6 | flaky 标记 | P1 | 判定口径见 §3.3 | 达标用例带不稳定标记；阈值可配（校准状态见 OP-1） |
| DF-7 | 质量汇总 | P2 | 窗口内通过率/时长/跑量/flaky 等 | 仪表板可用；权限不串 |

### 5.7 定时调度（SC）— P1

| ID | 需求 | 优先级 | 说明 | 合格标准 |
|----|------|--------|------|----------|
| SC-1 | 创建/修改/删除 | P1 | 绑 Profile + cron + IANA 时区 + 开关 | 非法表达式/时区不可存；Profile 必须存在 |
| SC-2 | 预览下次时间 | P1 | 创建前可看 | 给出未来 5 次触发时间 |
| SC-3 | 到点自动跑 | P1 | enabled 才跑 | 关闭不跑 |
| SC-4 | 立即触发 | P1 | 不等 cron | 产生新 Run 且归属触发者；Profile 已删则明确失败 |
| SC-5 | 防重复触发 | P1 | 同一触发点 | 同一触发点只产生一个 Run（数据库级原子认领） |

### 5.8 通知（NT）— P2（可选）

| ID | 需求 | 优先级 | 说明 | 合格标准 |
|----|------|--------|------|----------|
| NT-1 | 终态通知 | P2 | 发飞书卡片。启用条件三者齐备：部署配了公开地址、Run 关联 Profile、该 Profile 配了合法 Webhook | 满足条件即发；**发送失败/超时不得改变 Run 终态与落库**（BR-5） |
| NT-2 | 可整体关闭 | P2 | 不配公开地址或 Webhook | 主路径完全不受影响 |

### 5.9 AI 失败诊断（AI）— P2（可选）

| ID | 需求 | 优先级 | 说明 | 合格标准 |
|----|------|--------|------|----------|
| AI-1 | 结构化诊断 | P2 | 配好 Key 时对失败 Run 归类 | 产出六类根因之一（新增失败/历史 flaky/环境问题/断言不符/超时/权限路径）+ 置信度（高/中/低）+ 证据 + 是否疑似回归 + 建议；**只读，不改任何代码** |
| AI-2 | 无 Key / 调用失败降级 | P0 | 可选能力不得伤主路径，故为 P0 | 空 Key：不报服务器错误，明示未配置；LLM 调用或解析失败：降级为低置信兜底结果，不报服务器错误 |
| AI-3 | 无失败可分析 | P2 | 不编造 | 明确「无可分析内容」 |
| AI-4 | 限流 | P1 | 防刷爆费用 | 按用户滑动窗口限频（默认 10 次/60 秒，可配，0=关）；超额拒绝并给出重试等待时间 |
| AI-5 | 模型方可切换 | P2 | anthropic / openai，可指自建网关 | 诊断结构不因换供应商而变 |

### 5.10 Web 与 API（UI）— P0/P1/P2

| ID | 需求 | 优先级 | 说明 | 合格标准 |
|----|------|--------|------|----------|
| UI-1 | Web 主路径 | P0 | 登录、套件、Profile、触发、Run、报告/日志/对比、调度 | 不写脚本能完成 §4.1、§4.2（人工演示 + E2E 双验证） |
| UI-2 | 用户管理 UI | P1 | admin 用 | 非 admin 不可用 |
| UI-3 | 中英、主题 | P2 | 切换 | 常用页可用 |
| UI-4 | 键盘可用 | P2 | 无障碍底线 | 关键交互可全键盘完成（可聚焦 + 键盘激活）；对话框焦点受管理 |
| UI-5 | REST API | P0 | 与 UI 同权限模型 | 集成方可完成同等主路径 |
| UI-6 | OpenAPI | P1 | 机器可读契约 | 与真实行为一致 |
| UI-7 | 健康检查 | P0 | 数据库可达才就绪 | 可供部署探针 |

---

## 6. 关键业务规则（BR，容易扯皮，写死）

| ID | 主题 | 规则 |
|----|------|------|
| BR-1 | 执行器默认 | 默认 docker；subprocess 对普通用户默认关闭，开放须显式全局开关（生产建议关） |
| BR-2 | 套件独占 | user 不得对他人名下套件执行/更新/准备/删除；admin 不受限 |
| BR-3 | 对比范围 | 只有「同范围」（§3.3）才出 diff；范围不同 = 无基线 |
| BR-4 | 失败定义 | failed / error 算失败；skipped 不算（diff 与 flaky 同口径） |
| BR-5 | 可选能力无害（P0） | AI、通知关闭、失败或超时，**不得**阻断跑测、状态落库与查结果 |
| BR-6 | 单节点语义 | 崩溃恢复、调度去重按单实例拥有数据库设计；默认不承诺多副本 |
| BR-7 | 同源约束 | 浏览器 Cookie 登录只承诺同源部署形态 |
| BR-8 | 密钥门禁 | 空/弱密钥与弱管理员密码拒绝启动 |
| BR-9 | 无基线不误导 | 无可比基线时明示「无基线」，禁止把全部用例判成「新增」 |

---

## 7. 非功能需求（NF）

### 7.1 安全

| ID | 要求 |
|----|------|
| NF-1 | 启动安全门：密钥/管理员密码缺失、弱值或已知占位值 → 拒绝启动（即 BR-8） |
| NF-2 | 登录防爆破：见 §2.3（默认 5 次、指数退避 60→900 秒，可配） |
| NF-3 | 执行隔离底线：docker 执行满足非 root、无网络、能力全弃、只读根文件系统、内存/CPU/进程数上限；subprocess 退路满足资源上限 + 环境变量白名单。危险参数/环境变量/路径在创建时拦截。默认策略不得「等于没隔离」 |
| NF-4 | 外呼地址收敛（降 SSRF）：Git 地址仅 `https://` / `git@`；Webhook 仅 `https://` 且拒环回/私网 |
| NF-5 | 结果解析防护：XML 解析防 XXE；单报告输入设上限（10MB）防拖垮服务 |
| NF-6 | 密钥卫生：凭证加密落库（密钥自主密钥派生）、永不回显、不进命令行参数/URL/普通日志 |
| NF-7 | 高成本操作限流：AI 生成（AI-4）与单用户在途 Run（RN-9）均有默认限额 |

### 7.2 可靠

| ID | 要求 |
|----|------|
| NF-8 | 重启后无永久「排队/执行中」（RN-15） |
| NF-9 | 外部 AI / 通知故障不影响 Run 主状态（BR-5） |
| NF-10 | 数据库不可用时健康检查必须失败（UI-7） |
| NF-11 | 停机给在途任务落盘缓冲（RN-16） |

### 7.3 容量与定位

| ID | 要求 |
|----|------|
| NF-12 | 全局并发（默认 4）、单用户在途（默认 20）均可配置，且必须有安全默认值 |
| NF-13 | 输出有界：运行日志每流仅留存末尾 256KB；供 AI 分析的日志截断（默认 16KB） |
| NF-14 | 定位小团队单节点；**不承诺**大规模多活 SLA（超出即触发方向级变更） |

### 7.4 部署与配置

| ID | 要求 |
|----|------|
| NF-15 | 开发、生产两套可复现的标准部署路径 |
| NF-16 | 生产同源提供 UI + API |
| NF-17 | 数据库、产物、套件目录路径可配置、可整体备份 |
| NF-18 | 运行配置统一 `QARUNNER_*` 环境变量；完整清单以 `.env.example` 为准（配置的 SSOT） |

### 7.5 关键配置（运维核对用）

| 用途 | 变量（`QARUNNER_` 前缀） | 当前默认 |
|------|--------------------------|----------|
| 必填 | `SECRET_KEY`、`ADMIN_PASSWORD` | 无默认，弱值拒启动 |
| 路径 | `DB_PATH`、`ARTIFACTS_ROOT`、`TESTS_ROOT` | 见 `.env.example`（备份点） |
| 并发 | `MAX_CONCURRENCY` / `MAX_INFLIGHT_RUNS_PER_USER` | 4 / 20 |
| 超时 | `DEFAULT_TIMEOUT_SECONDS` | 1800 秒 |
| subprocess | `ALLOW_SUBPROCESS_FOR_NON_ADMINS` | false（生产保持） |
| Cookie | `COOKIE_SECURE` | false（生产 true） |
| AI | `AI_PROVIDER` / `AI_API_KEY` / `AI_POST_MAX_CALLS` 等 | Key 空=关闭；限流 10 次/60 秒 |
| flaky | `FLAKY_*` | 4 次观测 / 3 次翻转（OP-1 待校准） |
| 通知 | `PUBLIC_URL` | 空=整体关闭 |
| 停机 | `SHUTDOWN_DRAIN_TIMEOUT_SECONDS` | 30 秒 |

---

## 8. 假设与依赖（AS）

违反假设时，产品不承诺对应能力；部署方须自行评估。

| ID | 假设/依赖 | 违反时的后果 |
|----|-----------|--------------|
| AS-1 | 宿主 Docker daemon 可达（默认执行器的硬依赖） | 只剩受限 subprocess 退路，且默认仅 admin 可用 |
| AS-2 | 单实例部署（崩溃恢复、调度去重按单实例语义设计） | 多副本需自行保证仅一副本开崩溃恢复；属超出承诺的形态 |
| AS-3 | 部署面受信：平台进程可达 docker socket，**编排层沦陷≈宿主 root**；真正的隔离边界是执行容器，不是平台进程 | 平台自身暴露在不受信网络时，须由部署层（反代/网络策略）另行加固 |
| AS-4 | 浏览器登录依赖 UI 与 API 同源部署（BR-7） | 跨源部署下 Cookie 登录不可用 |
| AS-5 | 可选外部依赖按需可达：LLM API（AI 诊断）、飞书 Webhook（通知）、Allure 二进制（HTML 报告） | 对应能力降级或关闭，主路径不受影响（BR-5、RP-3） |
| AS-6 | 宿主时钟与 IANA 时区数据正确 | cron 触发时间不可信 |

---

## 9. 不做清单

变更下列任一项，属**方向级变更**（§0.4）：先改 [`DIRECTION.md`](DIRECTION.md) 再改本文。

1. CI：仓库事件驱动流水线、多 stage 编排
2. QA 平台：用例库、测试计划、缺陷、需求追溯
3. 多租户：组织/项目/计费隔离
4. 默认多副本多活架构
5. 独立 CLI 产品形态
6. AI 自动修改测试或业务代码
7. 完整审计中台、Prometheus 一等公民指标（除非单独立项）

---

## 10. 风险与开放项

### 10.1 风险登记（RK）

| ID | 风险 | 影响 | 缓解 / 接受理由 |
|----|------|------|------------------|
| RK-1 | flaky 阈值（4 观测/3 翻转）为保守占位，未经真实数据校准 | 误报/漏报不稳定用例 | 阈值可配（NF 表 `FLAKY_*`）；积累生产数据后回看（OP-1） |
| RK-2 | DooD 信任边界：平台进程持有 docker socket，等效宿主 root（AS-3） | 平台进程沦陷即宿主沦陷 | 平台输入面硬化（NF-3～NF-6）+ 部署面收敛；残余风险在「受信部署面」定位内接受 |
| RK-3 | 单节点单点故障 | 宕机期间调度不触发、结果不可查 | 可整体备份（NF-17）+ 快速重建；定位内接受（NF-14） |
| RK-4 | AI 外发面：失败日志片段外发 LLM 供应商，可能含敏感信息 | 数据外泄面扩大 | 默认关闭（Key 空）；日志截断 16KB；限流；可指自建网关（AI-5） |
| RK-5 | SQLite 单写者：高并发写吞吐受限 | 超大团队/高频场景变慢 | 单节点小团队定位内接受；超出即触发方向级变更（NF-14） |

### 10.2 开放项（OP，不阻塞当前基线）

| ID | 事项 | 处理 |
|----|------|------|
| OP-1 | flaky 默认阈值待真实数据校准 | 先可配置，积累数据后校准（挂 RK-1） |
| OP-2 | `FEATURES.md` 缺「通知」能力小节（其 §11「不做清单」中错误的「❌ 通知」行已随本文 v2.0 一并修正） | 后续为 FEATURES 补通知小节 |

> v1.3 开放项处置：原第 2 条「通知启用条件与实现对齐」已闭环——启用条件已在 NT-1 写死并经源码核对；原第 3 条并入 OP-2。

---

## 11. 验收标准（V）

| ID | 验收项 | 通过条件 | 覆盖需求 |
|----|--------|----------|----------|
| V1 | 主路径 | §4.1 可独立完成（人工演示 + E2E） | SU-1..8、PF-1/2、RN-1..11、RP-1..4、UI-1/5、SC-1..4 |
| V2 | 回归对比 | 同范围两次运行，diff 能区分新失败与修复；无基线时不误导 | DF-1..6、BR-3/4/9 |
| V3 | 权限 | 用户 B 拿不到用户 A 的数据与基线 | §2.2 全部、CR-4、DF-3、BR-2 |
| V4 | 默认安全 | 默认 docker；普通用户默认不能 subprocess；危险路径/参数被拒 | RN-3/4、SU-8、BR-1、NF-3..6 |
| V5 | 可选降级 | 无 AI Key、通知失败时，跑测与查结果正常 | NT-1/2、AI-2、BR-5、NF-9 |
| V6 | 部署 | 按部署文档启动，健康检查通过 | NF-15..18、UI-7 |
| V7 | 工程门禁 | 按仓库既有测试/覆盖率约定，不在此另立指标 | 全部（验证机制） |

---

## 12. 追溯矩阵

**完备性规则**：每条功能需求必须映射到至少一个目标；映射不到的即视为范围外候选，应触发 §0.4 需求级变更（收编或删除）。

| 目标 | 支撑需求 | 验收 | 度量锚（METRICS.md） |
|------|----------|------|----------------------|
| GO-1 接得进、跑得动 | SU-1..8、CR-1..3、RN-1..16、RP-1..5、UI-1/5/6/7 | V1、V6 | #4 平均时长、#5 跑量 |
| GO-2 可复用、可定时 | PF-1/2、SC-1..5 | V1 | #5 跑量 |
| GO-3 结果可解释 | RP-1..5、AI-1/3/5（增强） | V1 | — |
| GO-4 跨次可对比 | DF-1..7、RP-2、BR-3/4/9 | V2 | #1 通过率、#2 flaky 数 |
| GO-5 默认更安全 | RN-3/4、CR-2..4、SU-8、UI-2、§2.2、BR-1/2/8、NF-1..7 | V3、V4 | — |
| GO-6 部署够简单 | NT-2、AI-2/4、BR-5/6/7、NF-8..18、UI-7、AS-1..6 | V5、V6 | — |

UI-3/4（i18n、主题、键盘可用）为体验性增强，归 GO-1。

---

## 13. 修订记录

| 版本 | 日期 | 变更人 | 说明 |
|------|------|--------|------|
| 1.0 | 2026-07-11 | 维护者 | 工程向 SRS 初稿 |
| 1.1 | 2026-07-11 | 维护者 | 产品包装版 |
| 1.2 | 2026-07-11 | 维护者 | 精简直白版 |
| 1.3 | 2026-07-11 | 维护者 | PM 优化：补目标/对象/主路径/优先级/业务规则/验收表 |
| 2.0 | 2026-07-11 | Claude（PM 整理），合入即所有者批准 | 企业级治理升级：①新增文档控制（编号规范/变更管理/基线）、假设依赖、风险登记、追溯矩阵；②需求 ID 一次性重编并冻结（S→SU、C→CR、P→PF、R→RN、O→RP、D→DF、H→SC、N→NT、A→AI、U→UI；原 R8 拆为 RN-8 全局排队 + RN-9 单用户拒绝两条，此后 R9..R15 顺延为 RN-10..16）；③判定口径按源码精确化（同范围=四元组、flaky=4 观测/3 翻转）并数值化关键限额；④目标可度量化并挂钩 METRICS；⑤修正 v1.3 含混处（「以实现为准」、P0* 脚注、病句） |
